kind: Pod
apiVersion: v1
metadata:
  name: sa-demo
  namespace: expense
spec:
  # one pod can contain mutiple containers
  serviceAccount: expense-mysql-secret
  containers:
  - name: nginx
    image: nginx